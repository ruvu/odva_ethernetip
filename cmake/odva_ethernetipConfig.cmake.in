# - Config file for the odva_ethernetip package
# It defines the following variables
#  odva_ethernetip_INCLUDE_DIRS - include directories for FooBar
#  odva_ethernetip_LIBRARIES    - libraries to link against
#  odva_ethernetip_EXECUTABLE   - the bar executable

include(CMakeFindDependencyMacro)

# Find dependencies
find_dependency(console_bridge REQUIRED)

set(odva_ethernetip_INCLUDE_DIRS "@CONF_INCLUDE_DIRS@")

# Our library dependencies (contains definitions for IMPORTED targets)
include("${odva_ethernetip_DIR}/export_odva_ethernetip.cmake")

# These are IMPORTED targets created by odva_ethernetipTargets.cmake
set(odva_ethernetip_LIBRARIES odva_ethernetip)


